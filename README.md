# Projects-for-CN
cd into the backend folder and then install dependencies
(npm install json-server )
after that start the jsopn server :
json-server --watch db.json


its run on the port 3000

Endpoints:
http://localhost:3000/vendors
http://localhost:3000/bills



after that cd into the frontend folder
and install all dependencies and run the vite :npm run dev
it will host at http://localhost:5173/


